"""Package core — modules applicatifs restants (non migrés vers axiom/).

Le moteur a été extrait dans le package `axiom/` (Pilier 1, §5).
Ce package ne ré-exporte plus rien : importer directement depuis `axiom.*`.
"""
